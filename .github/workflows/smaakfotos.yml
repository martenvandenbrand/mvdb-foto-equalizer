name: Genereer smaakfoto's

on:
  workflow_dispatch:
    inputs:
      batch_size:
        description: "Aantal flessen per run (kostenbeheersing)"
        type: choice
        options: ["1", "10"]
        default: "1"
      handle:
        description: "1 specifieke fles (URL-slug). Ingevuld = alleen deze fles"
        type: string
        default: ""
      dry_run:
        description: "Dry-run: wel genereren (kost credits) + artifact, niet uploaden/taggen"
        type: boolean
        default: true
      image_model:
        description: "Beeldmodel"
        type: choice
        options: ["gpt-image-1.5", "gpt-image-1-mini"]
        default: "gpt-image-1.5"
      image_quality:
        description: "Kwaliteit"
        type: choice
        options: ["low", "medium", "high"]
        default: "high"
      flavor_size:
        description: "Grootte van de smaken in px (subtiliteit)"
        type: choice
        options: ["220", "260", "320", "400", "480", "560"]
        default: "260"
      cutout_source:
        description: "Bron van de smaakbeelden"
        type: choice
        options: ["gpt", "stock"]
        default: "gpt"
      bypass_cache:
        description: "Cache negeren en smaken opnieuw maken (ook bij gpt)"
        type: boolean
        default: false

jobs:
  smaakfotos:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Installeer dependencies
        run: pip install -r requirements.txt

      # Alleen nodig voor stock-modus (echte foto's + achtergrond weg)
      - name: Installeer rembg (stock-modus)
        if: ${{ inputs.cutout_source == 'stock' }}
        run: pip install rembg onnxruntime

      # Hergebruikt smaak-uitsnedes tussen runs -> unieke smaken maar 1x betalen
      - name: Cache smaak-uitsnedes
        uses: actions/cache@v4
        with:
          path: flavor_cache
          key: flavor-cache-${{ github.run_id }}
          restore-keys: flavor-cache-

      - name: Genereer smaakfoto's
        env:
          SHOP: ${{ secrets.SHOP }}
          SHOPIFY_CLIENT_ID: ${{ secrets.SHOPIFY_CLIENT_ID }}
          SHOPIFY_CLIENT_SECRET: ${{ secrets.SHOPIFY_CLIENT_SECRET }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          BATCH_SIZE: ${{ inputs.batch_size }}
          HANDLE: ${{ inputs.handle }}
          DRY_RUN: ${{ inputs.dry_run }}
          OPENAI_IMAGE_MODEL: ${{ inputs.image_model }}
          IMAGE_QUALITY: ${{ inputs.image_quality }}
          FLAVOR_PX: ${{ inputs.flavor_size }}
          CUTOUT_SOURCE: ${{ inputs.cutout_source }}
          PEXELS_API_KEY: ${{ secrets.PEXELS_API_KEY }}
          BYPASS_CACHE: ${{ inputs.bypass_cache }}
        run: python smaakfoto_generator.py

      - name: Gegenereerde foto's als artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: smaakfotos
          path: backup_smaak/
          if-no-files-found: ignore
